import 'package:flutter/material.dart';

class RoutesContactUs extends StatelessWidget {
  const RoutesContactUs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(
          child: Text('Contact Us'),
        ),
      ),
    );
  }
}
